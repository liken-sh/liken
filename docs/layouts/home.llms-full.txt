{{- /*
The whole manual in one file, for an agent that takes one fetch
instead of a crawl. Pages appear in reading order: guides first, then
the reference, each as its authored Markdown. Every page has its own
top-level heading, so the headings delimit the pages.
*/ -}}
{{- $docs := .Site.GetPage "/docs" -}}
{{- range $docs.Sections }}
{{- range .RegularPages }}
{{ .RawContent }}
{{- end }}
{{- end }}
