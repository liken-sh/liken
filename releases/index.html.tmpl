{{/*
The channel's front page: what the channel is, how it is laid out, and
every release it holds.

The list holds every version, and it never truncates. A channel that
hides its old releases makes a rollback undiscoverable, and a rollback
is exactly the moment when an operator needs to read a version they do
not already know the name of.

This page is also the channel's error document, so a mistyped path
lands here. That is another reason the whole list belongs on it.
*/}}
{{template "head" .}}
<h1>{{.Mark}} <span><code>liken</code> releases</span></h1>

<p>This is the release channel for
<a href="https://liken.sh"><code>liken</code></a>, a small operating
system that boots a machine directly into Kubernetes. The channel is
plain object storage: every published file is public, and a
published release's artifacts never change.</p>

<p><a href="https://liken.sh/docs/guides/install/">The install
guide</a> gives the steps from a release to a running cluster.
<a href="https://liken.sh/docs/reference/release-channel/">The channel
reference</a> describes this layout in full.</p>

<h2>Releases</h2>

<p>{{len .Releases}} published, newest first. Open one to get the
catalog entry to copy, or read
<a href="/versions.yaml"><code>versions.yaml</code></a> for the same
list in one file that a script can parse.</p>

<table>
<tr><th>Version</th><th>Kernel</th><th>k3s</th><th>Release document</th></tr>
{{range .Releases}}
<tr>
  <td{{if .IsLatest}} class="latest"{{end}}><a href="/{{.Version}}/">{{.Version}}</a></td>
  <td>{{.Component "kernel"}}</td>
  <td>{{.Component "k3s"}}</td>
  <td class="digest">{{.Digest}}</td>
</tr>
{{end}}
</table>

<h2>The layout</h2>

<ul>
  <li><a href="/channel.yaml"><code>channel.yaml</code></a> names the
  newest published version. A cluster polls it for a newer version,
  so it stays one small file however many releases exist.</li>
  <li><a href="/versions.yaml"><code>versions.yaml</code></a> lists
  every release the channel holds, each with the digest of its
  release document. No machine reads this one.</li>
  <li><code>/&lt;version&gt;/release.yaml</code> lists a release's
  artifacts, each with its sha256 digest, and the artifacts are in
  the same directory.</li>
  <li><code>/&lt;version&gt;/LICENSES.md</code> carries the
  third-party notices for everything a release redistributes.</li>
  {{if .Sources}}<li><a href="/sources/"><code>/sources/</code></a>
  serves the corresponding source for the GPL- and LGPL-licensed
  components, beside the binaries built from it.</li>{{end}}
</ul>

<h2>Trust</h2>

<p>The channel needs no authentication and gives no guarantees of
its own. A machine verifies every byte it downloads against the
digest that its own <code>Cluster</code> document pins. Each
release's page prints that digest.</p>

{{template "foot" .}}
